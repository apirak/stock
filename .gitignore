# Secrets & credentials — never commit these
.env
*.pem

# Runtime data & generated previews (src/data/daily_log.csv is the tracking
# database — it lives on disk; keep your own backup if it matters to you)
src/data/
src/output/
__pycache__/
*.pyc
.venv/
.pytest_cache/

.DS_Store
